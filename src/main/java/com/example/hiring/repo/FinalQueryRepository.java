package com.example.hiring.repo;

import com.example.hiring.model.FinalQueryRecord;
import org.springframework.data.jpa.repository.JpaRepository;

public interface FinalQueryRepository extends JpaRepository<FinalQueryRecord, Long> { }
